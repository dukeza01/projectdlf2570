{
  "name": "dlf-pdf-export-backend",
  "version": "1.0.0",
  "description": "Headless-browser (Puppeteer) PDF export backend for DLF-Project2570 — renders the same HTML the front-end already builds into a proper, selectable-text A4 PDF with a repeating header (logo) and footer (page numbers).",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "puppeteer": "^22.15.0"
  }
}
